
def prime_numbers(n):
    prm_num =[]
    for i in range(1, n+1):
        prime = True
        for ii in range(2, i):
           if (i % ii == 0):
              prime = False
        if prime:
           prm_num.append(i)
    return prm_num

print(prime_numbers(100))
